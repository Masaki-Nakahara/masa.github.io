<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>LIST APP</title>
    <link rel="stylesheet" href="/css/style.css">
    <script src="/send_url.js"></script>
  </head>

  <body>
    <div>
    <%
    personGets.forEach((personGet)=> {
        personPays.forEach((personPay)=>{ 
            if(personGet.extra > personPay.shortage){
                personGet.extra -= personPay.shortage;
    %>
                <p><%= personPay.name%>が<%= personGet.name%>へ<%= personPay.shortage%>円払う</p>
    <%
                personPay.shortage = 0;
            }else if(personGet.extra < personPay.shortage){
                personPay.shortage -= personGet.extra;
    %>
                <p><%= personPay.name%>が<%= personGet.name%>へ<%= personGet.extra%>円払う</p>
    <%      
                personGet.extra = 0;
              
    
            }else{
    %>
                <p><%= personPay.name%>が<%= personGet.name%>へ<%= personGet.extra%>円払う</p>
    <%           
                personPay.shortage = 0;
                personGet.extra = 0;
            }
        });
    }); 
    %>
       
    
    
    
    
    

    </div>
    <a href="/main">一覧に戻る</a>
  </body>
</html>